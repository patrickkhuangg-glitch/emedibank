import type { Feedback } from '@/lib/interviews/marking-validation'
export function InterviewFeedback({feedback}:{feedback:Feedback}){return <section className="mt-6 space-y-5" aria-label="Human-approved feedback">
 <div><h3 className="font-display text-2xl font-semibold">Approved practice feedback</h3><p className="mt-2 font-semibold">{feedback.overall.score} / 7 · {feedback.overall.band}</p><p className="mt-2 whitespace-pre-wrap leading-7">{feedback.overall.summary}</p><p className="mt-2 text-sm text-muted">Practice performance only; this does not predict admissions outcomes.</p></div>
 <div className="grid gap-4 sm:grid-cols-2">{feedback.domains.map(d=><article key={d.key} className="rounded-2xl border border-border p-4"><h4 className="font-semibold">{d.label} · {d.applicable?`${d.score} / 7`:'Not applicable'}</h4><p className="mt-2 whitespace-pre-wrap text-sm leading-6">{d.comment}</p><ul className="mt-2 list-disc space-y-1 pl-5 text-sm">{d.evidence.map((e,i)=><li key={i}>{e}</li>)}</ul></article>)}</div>
 <div className="grid gap-5 sm:grid-cols-2"><div><h4 className="font-semibold">Strengths</h4><ul className="mt-2 list-disc space-y-2 pl-5">{feedback.strengths.map((s,i)=><li key={i}>{s}</li>)}</ul></div><div><h4 className="font-semibold">Priority improvements</h4><ul className="mt-2 list-disc space-y-2 pl-5">{feedback.priorities.map((s,i)=><li key={i}>{s}</li>)}</ul></div></div>
 <div className="rounded-2xl bg-surface-muted p-5"><h4 className="font-semibold">Next-practice task</h4><p className="mt-2">{feedback.practice_task}</p></div><p className="text-sm text-muted">Reviewed and approved by an EMeducate reviewer.</p>
 </section>}
